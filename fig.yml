postgresql:
  image: postgres:9
redis:
  image: redis:2.8
web:
  build: .
  command: bin/rails server -e production
  links:
   - postgresql
   - redis
  ports:
   - "3000:3000"
  environment:
    POSTGRESQL_USER: postgres
